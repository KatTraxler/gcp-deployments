apiVersion: v1
kind: Service
metadata:
  name: {{ include "avml.fullname" . }}
  labels:
    {{- include "avml.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "avml.selectorLabels" . | nindent 4 }}
